https://www.acwing.com/problem/content/609/

```c++
#include <cstdio>

double a, b, c;

int main() {
  scanf("%lf %lf %lf", &a, &b, &c);
  printf("MEDIA = %.1lf\n", (a * 2 + b * 3 + c * 5) / (2 + 3 + 5));
  return 0;
}
```
