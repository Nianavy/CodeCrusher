https://www.acwing.com/problem/content/808/

```c++
#include <cstdio>

double add(double x, double y) {
  return x + y;
}

int main() {
  double a, b;
  scanf("%lf %lf", &a, &b);
  printf("%.2lf\n", add(a, b));
  return 0;
}
```
